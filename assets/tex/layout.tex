% --- Responsive Dimensions ---

\begin{frame}{Responsive Dimensions}
    All layout dimensions use relative units, so every element adapts to the
    chosen aspect ratio without manual adjustment.

    \input{tex/layout/dimensions.tex}
\end{frame}

% --- Flexible Spacing ---

\begin{frame}{Flexible Spacing}
    Vertical spacing uses \LaTeX{} glue (natural $\pm$ stretch/shrink) so the
    engine can redistribute space when a slide is tight or loose.

    \input{tex/layout/spacing.tex}
\end{frame}
