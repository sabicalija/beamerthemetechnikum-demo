\begin{figure}
    \centering
    \begin{tikzpicture}[element/.style={draw, rounded corners, minimum width=2cm, minimum height=1cm},
            label/.style={fhtw-blue, thick, dashed}, node distance=2cm, >=Latex]
        
        % Nodes — always placed to keep layout stable
        \node[element, fill=fhtw-blue!15] (sensor) {Sensor};
        \onslide<2->{\node[element, fill=fhtw-green!15, right=of sensor] (mcu) {\acs{mcu}};}
        \onslide<3->{\node[element, fill=fhtw-gray!15, right=of mcu] (cloud) {Cloud};}

        % Edges
        \onslide<2->{\draw[->] (sensor) -- node[above] {\acs{i2c}} (mcu);}
        \onslide<3->{\draw[->] (mcu) -- node[above] {\acs{mqtt}} (cloud);}

        % Annotations
        \onslide<4->{\draw[<->, label] (sensor.south) -- ++(0,-1) -| (cloud.south)
            node[pos=0.25, below] {End-to-end data flow};}
    \end{tikzpicture}
    \caption{Stable layout --- nodes reserve space while invisible}
\end{figure}