\begin{figure}
    \centering
    \begin{tikzpicture}[element/.style={draw, rounded corners, minimum width=2cm, minimum height=1cm},
            label/.style={fhtw-blue, thick, dashed}, node distance=2cm, >=Latex]
        
        % Nodes
        \node[element, fill=fhtw-blue!15] (sensor) {Sensor};
        \node<2->[element, fill=fhtw-green!15, right=of sensor] (mcu) {\acs{mcu}};
        \node<3->[element, fill=fhtw-gray!15, right=of mcu] (cloud) {Cloud};

        % Edges
        \draw<2->[->] (sensor) -- node[above] {\acs{i2c}} (mcu);
        \draw<3->[->] (mcu) -- node[above] {\acs{mqtt}} (cloud);

        % Annotations
        \draw<4->[<->, label] (sensor.south) -- ++(0,-1) -| (cloud.south)
            node[pos=0.25, below] {End-to-end data flow};
    \end{tikzpicture}
    \caption{Progressive build-up of an \acs{iot} architecture}
\end{figure}